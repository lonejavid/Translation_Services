# Use Python 3.11 (or 3.10 / 3.9). Coqui TTS on PyPI requires Python <3.12.
python-dotenv>=1.0.0
# eval_type_backport lets pydantic v2 handle `X | Y` and `X | None` annotations on Python 3.9
eval_type_backport>=0.1.3
# chatterbox-tts → gradio 5.44.1 requires fastapi>=0.115.2
fastapi>=0.115.2,<0.116
uvicorn[standard]==0.29.0
slowapi>=0.1.9
yt-dlp>=2024.3.10
# mlx-whisper requires Python >=3.10 (via mlx>=0.11); omitted for Python 3.9 compat.
# Install manually if using Python 3.10+: pip install mlx-whisper
# The transcriber falls back to faster-whisper when mlx-whisper is not available.
# chatterbox-tts 0.1.3 is the latest available on PyPI. 0.1.6 does not exist.
transformers==4.46.3
torch>=2.0.0
torchaudio>=2.0.0
# TTS (Coqui) is installed with --no-deps in electron/main.js to prevent its
# numpy==1.22.0 pin (Python 3.9) from making pip's resolver fail.
pydub>=0.25.1
silero-vad>=5.1.0
webrtcvad>=2.0.10
python-multipart>=0.0.9
aiofiles>=23.2.1
httpx>=0.27.0
sentencepiece>=0.2.0
# sacremoses pulls in pkuseg which requires C++ compilation and fails on Python 3.9 + macOS ARM64.
# sacremoses is not used directly in our code — omit it.
scipy>=1.11.0
soundfile>=0.12.0
pyrnnoise>=0.4.0
# TTS (Coqui) pins numpy==1.22.0 on Python 3.9; scikit-learn 1.5+ needs numpy>=1.25.
# Cap scikit-learn at 1.4.x (compatible with numpy 1.22) and allow numpy up to 2.x
# so pip can find a solution regardless of Python version.
numpy>=1.22.0
scikit-learn>=1.3.0,<1.5.0
deep-translator>=1.11.0
beautifulsoup4>=4.12.0
edge-tts>=6.1.0
# chatterbox-tts 0.1.3 works with librosa 0.11.x
librosa==0.11.0
noisereduce>=3.0.0
pyloudnorm>=0.1.1
demucs>=4.0.0
# OpenVoice v2 — optional; install from git if needed (see comments in older docs).
eng_to_ipa>=0.0.2
cn2an>=0.5.22
wavmark>=0.0.3
faster-whisper>=1.0.0
whisper-timestamped>=1.15.4
# chatterbox-tts is installed with --no-deps in Electron setup (see electron/main.js)
# to prevent it pulling pkuseg==0.0.25, which has no wheel for Python 3.9 ARM64.
